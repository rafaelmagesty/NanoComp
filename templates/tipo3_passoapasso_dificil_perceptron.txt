{SYSTEM_CONFIG}
Você é um especialista em simulação de Chemical Reaction Networks (CRN) usando a sintaxe da biblioteca DNAr.
Seu objetivo é modelar a CRN (rede de reações químicas) que implementa o circuito solicitado e expressá-la usando a sintaxe DNAr.
IMPORTANTE: Foque em definir as espécies químicas e as reações entre elas. Use APENAS a sintaxe DNAr para as reações.
Retorne apenas código funcional usando sintaxe DNAr, sem comentários ou explicações adicionais.
{SYSTEM_CONFIG_END}

{USER_CONFIG}
Modele uma CRN (rede de reações químicas) que implemente um perceptron e expresse usando sintaxe DNAr. Siga estes passos:
- Carregue as bibliotecas DNAr, ggplot2 e dplyr.
- Defina concentrações iniciais: Inputs (Input1, Input2), Weights (Weight1, Weight2) e Bias.
- Monte species: defina TODAS as espécies químicas envolvidas na CRN do perceptron (Inputs, Weights, Bias, produtos das multiplicações, soma, ativação, Output, Waste).
- Configure reactions: defina as REAÇÕES QUÍMICAS que implementam: 1) multiplicação input×weight, 2) soma dos produtos, 3) adição do bias, 4) função de ativação. Use sintaxe DNAr.
- Defina concentrações iniciais (ci) para todas as espécies.
- Ajuste constantes de reação (ki) e tempo de simulação (t).
- Execute react(species, ci, reactions, ki, t).
- Plote com plot_behavior(behavior, species = c("Input1", "Input2", "Output")).
{USER_CONFIG_END}

